name: Vault Key/Value
description: Export secrets from Hashicorp Vault (Key/Value Secret Engine) as environment variables.
author: https://adabay.rocks
requirements: []
configuration:
  properties:
    secrets:
      type: array
    token_file_path:
      type: string
    secret_path:
      type: string
    secret_key:
      type: string
    exported_env_variable_name:
      type: string
